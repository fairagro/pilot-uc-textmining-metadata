## This directory includes the code to pre-annotate thd text files of each source

Run the following python code
```bash
python annotate_inception.py \
    --input_dir  directory/to/text/files\
    --output_dir directory/to/save/inception/files
```